import { Card } from "@repo/ui";

export function DashboardPage() {
  return (
    <Card>
      <h2>Dashboard Microfrontend</h2>
      <p>This content is rendered inside the shared shell layout.</p>
    </Card>
  );
}
